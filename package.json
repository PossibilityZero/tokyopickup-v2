{
    "name": "delete-test-01",
    "description": "delete-test-01-description",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "eslint": "^8.22.0",
        "eslint-config-semistandard": "^17.0.0",
        "jest": "^26.6.3",
        "npx": "^10.2.2"
    },
    "scripts": {
        "lint": "eslint src",
        "test": "jest --testPathIgnorePatterns=.aws-sam/build --silent"
    }
}
